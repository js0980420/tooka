---
'@tooka/core': patch
---

Make the sidebar bigger by default (320px, up from 264px) with larger row text and icons, and add a drag handle to resize it — width persists per browser, with keyboard resize and a double-click-to-reset shortcut, mirroring the thumbnail rail's resize behavior.
