---
'@tooka/core': minor
---

PNG export variants can declare a `crop` size to center-crop the capture (e.g. a true 1080×1080 IG square), with the editor preview dimming the cropped area and marking the profile-grid side safe zone.
