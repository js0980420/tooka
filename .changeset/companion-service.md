---
'@tooka/core': minor
---

Add `tooka companion`: a standalone local service exposing the AI agent API (with an origin allowlist) so a hosted web app can drive the user's own CLI, files, and sessions.
