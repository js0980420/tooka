---
'@tooka/core': minor
---

Add a model picker to the AI chatbot: connected providers expose their available models (Codex reads the CLI's live model list) and the choice is passed through to the agent run.
