---
"@tooka/core": patch
---

Move react/react-dom to peerDependencies, drop the shadcn runtime dependency (its tailwind.css is vendored), and ship src/shared + src/publishing in the published package.
