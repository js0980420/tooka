---
'@tooka/core': minor
---

`tooka companion` now runs the full dev server — compiled slides, live previews, HMR, and every API — so a hosted shell can deep-link into the local studio; chatbot guidance buttons open studio pages there in hosted builds.
