---
'@tooka/core': minor
---

`tooka companion` now serves the full dev-server API surface (slides, folders, edit, comments, assets, connects, publish, update, svgl) — not just the agent — so a hosted web app can drive the whole local project.
