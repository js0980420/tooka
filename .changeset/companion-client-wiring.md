---
'@tooka/core': minor
---

Hosted builds can point the AI chatbot at a local companion: set VITE_TOOKA_COMPANION at build time to mount the chatbot in production and route agent calls to the user's machine.
